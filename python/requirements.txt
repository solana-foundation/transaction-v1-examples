# Exact versions `_py-install` resolves against, so a dependency release cannot
# change what CI builds. Applied with `pip install -c`, so this constrains the
# resolution rather than defining it — `pyproject.toml` remains the dependency
# list. Regenerate with `.venv/bin/pip freeze --exclude-editable | sort`.
Pygments==2.21.0
anyio==4.14.2
ast_serialize==0.8.0
certifi==2026.7.22
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
iniconfig==2.3.0
jsonalias==0.1.1
librt==0.15.0
mypy==2.3.1
mypy_extensions==1.1.0
packaging==26.3
pathspec==1.1.1
pluggy==1.6.0
pytest==9.1.1
ruff==0.16.3
solders==0.29.0
typing_extensions==4.16.0
