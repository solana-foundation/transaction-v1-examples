# kit-plugins

Reserved for transaction v1 examples built on the `@solana/kit` plugin clients.

Nothing lives here yet. The `@solana/kit` examples are in [`../kit`](../kit), and
the `@solana/web3.js` ones in [`../web3js`](../web3js).
